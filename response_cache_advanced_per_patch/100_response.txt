<response>
  <success>true</success>
  <code><![CDATA[
  ```java
  @Override
  public void setGrabMouse(final boolean grab) {
    this.grabMouse = grab;

    if (this.isGrabMouse()) {
      if (Game.window() != null) {
        Game.window().cursor().hideDefaultCursor();
      }
    } else if (Game.window() != null && !Game.window().cursor().isVisible()) {
      Game.window().cursor().showDefaultCursor();
    }
  }
  ```
    ]]>
  </code>
</response>