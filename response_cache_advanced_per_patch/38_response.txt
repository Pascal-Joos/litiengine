<response>
  <success>true</success>
  <code><![CDATA[
  ```java
  public void setCamera( final ICamera cam) {
      if (this.camera() != null && Game.loop() != null) {
        Game.loop().detach(camera);
      }

      camera = cam;

      if (cam != null && !Game.isInNoGUIMode() && Game.loop() != null) {
        Game.loop().attach(cam);
        cam.updateFocus();
      }
  }
  ```
    ]]>
  </code>
</response>