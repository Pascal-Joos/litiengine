<response>
  <success>true</success>
  <code><![CDATA[
  ```java
  @Override
  public Point2D getMapLocation() {
    return Game.world().camera() != null
        ? Game.world()
            .camera()
            .getMapLocation(
                new Point2D.Double(
                    this.getLocation().getX() / Game.world().camera().getRenderScale(),
                    this.getLocation().getY() / Game.world().camera().getRenderScale()))
        : null;
  }
  ```
    ]]>
  </code>
</response>