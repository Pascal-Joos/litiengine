In the method below, the expression At the call site: ```java if (this.isInfiniteMap()) { TmxMap map = (TmxMap) this.getMap(); ... } ``` the method `this.getMap()` is invoked inside the `ImageLayer.isInfiniteMap()` guard: ```java private boolean isInfiniteMap() { return this.getMap() != null && this.getMap().isInfinite() && this.getMap() instanceof TmxMap; } ``` The `if (this.isInfiniteMap())` condition can be `true` only if all of the following are true simultaneously: 1. `this.getMap() != null` 2. `this.getMap().isInfinite()` (which also implies `this.getMap()` is not null when evaluated) 3. `this.getMap() instanceof TmxMap` So at the specific call site that is executed only when `isInfiniteMap()` is `true`, we know by control flow: - `this.getMap()` is guaranteed non‑null. - `this.getMap()` is guaranteed to be an instance of `TmxMap`. Therefore, along any execution path that reaches: ```java TmxMap map = (TmxMap) this.getMap(); ``` `this.getMap()` cannot be null, and the cast will succeed. The underlying implementation: ```java @Override public IMap getMap() { return this.parentMap; } ``` may return null in general (if `parentMap` is null), but that case is excluded for this particular call site by the `isInfiniteMap()` condition. Hence, at this specific call site, the result of `this.getMap()` cannot be null. is not nullable at the point it is used. ```java map ``` Please rewrite the method so that at the line "@Override public int getOffsetX() { if (this.isInfiniteMap()) { TmxMap map = (TmxMap) this.getMap(); return super.getOffsetX() - map.getChunkOffsetX() * map.getTileWidth(); } return super.getOffsetX(); }", at the occurrence of: " return super.getOffsetX() - map.getChunkOffsetX() * map.getTileWidth();" is replaced with `Nullability.castToNonnull(map)` See the example below, for expression `foo.getBar()`, wrap it with `Nullability.castToNonnull(foo.getBar())`. ```java public t(){ String l = foo.getBar(); } ``` will be: ```java public t(){ String l = Nullability.castToNonnull(foo.getBar()); } ``` ### Instructions: Do not import any additional classes. Do not add Java comments, but also do not remove any preexisting comments. Only modify relevant lines as required. Do not address any other issues. Return only the modified method wrapped in the XML format shown below. Place the Java code inside the <code> tag using a CDATA block with ```java formatting. Just give me the fixed code snippet in XML format in <response> tag like the example below. I will apply the fix to my codebase. Do not import any additional libraries or classes or add java comments. If you can provide the fixed code snippet, please provide it in the following format and place the code snippet in the <code> tag within ```java block. <response> <success>true</success> <code><![CDATA[ ```java Your fixed code snippet here ``` ]]> </code> </response>