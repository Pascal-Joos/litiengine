In the method below, the expression entity.getLocation() is not nullable at the point it is used. ```java public boolean move(final IMobileEntity entity, final double angle, final double distance) { final Point2D newPosition = GeometricUtilities.project(entity.getLocation(), angle, distance); return this.move(entity, newPosition); } ``` Please rewrite the method so that at the line " final Point2D newPosition = GeometricUtilities.project(entity.getLocation(), angle, distance);", at the occurrence of: "entity.getLocation()" is replaced with `Nullability.castToNonnull(entity.getLocation())` See the example below, for expression `foo.getBar()`, wrap it with `Nullability.castToNonnull(foo.getBar())`. ```java public t(){ String l = foo.getBar(); } ``` will be: ```java public t(){ String l = Nullability.castToNonnull(foo.getBar()); } ``` ### Instructions: Do not import any additional classes. Do not add Java comments, but also do not remove any preexisting comments. Only modify relevant lines as required. Do not address any other issues. Return only the modified method wrapped in the XML format shown below. Place the Java code inside the <code> tag using a CDATA block with ```java formatting. Just give me the fixed code snippet in XML format in <response> tag like the example below. I will apply the fix to my codebase. Do not import any additional libraries or classes or add java comments. If you can provide the fixed code snippet, please provide it in the following format and place the code snippet in the <code> tag within ```java block. <response> <success>true</success> <code><![CDATA[ ```java Your fixed code snippet here ``` ]]> </code> </response>