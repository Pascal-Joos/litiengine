In the method below, the expression Within this method, all code paths that reach the expression `Game.window().getResolution()` at: } else if (Game.window().getResolution().getWidth() - mouseLocation.getX() are preceded by an explicit null check: if (Game.window().getResolution() == null) { return; } This means: 1. The method returns immediately if `Game.window().getResolution()` is null. 2. Therefore, any subsequent code (including the `else if` using `Game.window().getResolution().getWidth()`) can only execute if `Game.window().getResolution()` was non-null at the time of the check. Assuming: - `Game.window()` itself is non-null (the method uses it directly without checks), and - `getResolution()` is a pure accessor that does not change between the null check and the later use within this single method execution (i.e., no concurrent modification or side-effect that could set it back to null in another thread or via callbacks inside the same method), then, from the perspective of this method’s control flow, the expression `Game.window().getResolution()` cannot be null when it is used in that `else if` condition. If external concurrent changes are possible (e.g., another thread resetting the resolution to null between the null check and the use), that would be a broader system-level concern, not detectable from this snippet alone. Under normal single-threaded or stable-API assumptions, it is safe. Therefore, given the code provided and standard assumptions, it cannot be null at that point. is not nullable at the point it is used. ```java Game.window().getResolution() ``` Please rewrite the method so that at the line "private void handleFreeFlightCamera() { if (Game.world().environment() == null || Game.world().environment().getMap() == null) { return; } if (Game.window().getResolution() == null) { return; } final Point2D mouseLocation = Input.mouse().getLocation(); final double scrollSpeed = this.getVelocity() / (double) Game.loop().getTickRate() * Game.config().input().getMouseSensitivity(); double x = this.getFocus().getX(); double y = this.getFocus().getY(); double deltaX = 0; if (mouseLocation.getX() < this.getScrollPadding()) { deltaX -= scrollSpeed; } else if (Game.window().getResolution().getWidth() - mouseLocation.getX() < this.getScrollPadding()) { deltaX += scrollSpeed; } double deltaY = 0; if (mouseLocation.getY() < this.getScrollPadding()) { deltaY -= scrollSpeed; } else if (Game.window().getResolution().getHeight() - mouseLocation.getY() < this.getScrollPadding()) { deltaY += scrollSpeed; } x += deltaX; y += deltaY; this.setFocus(new Point2D.Double(x, y)); }", at the occurrence of: " } else if (Game.window().getResolution().getWidth() - mouseLocation.getX()" is replaced with `Nullability.castToNonnull(Game.window().getResolution())` See the example below, for expression `foo.getBar()`, wrap it with `Nullability.castToNonnull(foo.getBar())`. ```java public t(){ String l = foo.getBar(); } ``` will be: ```java public t(){ String l = Nullability.castToNonnull(foo.getBar()); } ``` ### Instructions: Do not import any additional classes. Do not add Java comments, but also do not remove any preexisting comments. Only modify relevant lines as required. Do not address any other issues. Return only the modified method wrapped in the XML format shown below. Place the Java code inside the <code> tag using a CDATA block with ```java formatting. Just give me the fixed code snippet in XML format in <response> tag like the example below. I will apply the fix to my codebase. Do not import any additional libraries or classes or add java comments. If you can provide the fixed code snippet, please provide it in the following format and place the code snippet in the <code> tag within ```java block. <response> <success>true</success> <code><![CDATA[ ```java Your fixed code snippet here ``` ]]> </code> </response>