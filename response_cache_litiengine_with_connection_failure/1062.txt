In the method below, the expression We are asked about the specific call site: ```java final BufferedImage dimg = getCompatibleImage(w, h) ``` as it appears in: ```java public static BufferedImage verticalFlip(final BufferedImage img) { final int w = img.getWidth(); final int h = img.getHeight(); if (w == 0 || h == 0) { return img; } final BufferedImage dimg = getCompatibleImage(w, h); final Graphics2D g = dimg.createGraphics(); g.drawImage(img, 0, 0 + h, w, -h, null); g.dispose(); return dimg; } ``` So for this call site: - The arguments to `getCompatibleImage` are `w` and `h`, where: - `w = img.getWidth()` - `h = img.getHeight()` - Immediately before the call, there is a guard: ```java if (w == 0 || h == 0) { return img; } ``` This guarantees that, when execution reaches the call `getCompatibleImage(w, h)`, both `w` and `h` are strictly non‑zero (i.e., `w != 0` and `h != 0`). Now look at the implementation of `getCompatibleImage`: ```java public static BufferedImage getCompatibleImage(final int width, final int height) { if (width == 0 || height == 0) { return null; } if (graphicsConfig == null) { final GraphicsEnvironment env = GraphicsEnvironment.getLocalGraphicsEnvironment(); final GraphicsDevice device = env.getDefaultScreenDevice(); graphicsConfig = device.getDefaultConfiguration(); } return graphicsConfig.createCompatibleImage(width, height, Transparency.TRANSLUCENT); } ``` All return paths: 1. `if (width == 0 || height == 0) { return null; }` 2. Otherwise, compute/init `graphicsConfig` and then: ```java return graphicsConfig.createCompatibleImage(width, height, Transparency.TRANSLUCENT); ``` For this particular call site: - Because of the prior check in `verticalFlip`, the first branch (`return null;`) is *unreachable* for this invocation: `width` and `height` are the nonzero `w` and `h`. - The remaining path returns the result of `graphicsConfig.createCompatibleImage(...)`. Per the analysis rules, we are not allowed to assume behavior of methods whose declarations we do not see; however, here that only affects whether `createCompatibleImage` might itself return `null`. Under the given rules, we must not speculate that `createCompatibleImage` returns `null` in the absence of its declaration; we only treat explicit `return null;` paths in the provided code as possible null returns. Thus, based solely on the provided implementation of `getCompatibleImage` and the guaranteed non‑zero arguments at this call site, the only explicit `return null;` is ruled out, and there is no further `return null;` in the method body. Therefore, for this specific invocation `getCompatibleImage(w, h)` in `verticalFlip`, the method cannot return `null` according to the given constraints. is not nullable at the point it is used. ```java dimg ``` Please rewrite the method so that at the line "public static BufferedImage verticalFlip(final BufferedImage img) { final int w = img.getWidth(); final int h = img.getHeight(); if (w == 0 || h == 0) { return img; } final BufferedImage dimg = getCompatibleImage(w, h); final Graphics2D g = dimg.createGraphics(); g.drawImage(img, 0, 0 + h, w, -h, null); g.dispose(); return dimg; }", at the occurrence of: " final Graphics2D g = dimg.createGraphics();" is replaced with `Nullability.castToNonnull(dimg)` See the example below, for expression `foo.getBar()`, wrap it with `Nullability.castToNonnull(foo.getBar())`. ```java public t(){ String l = foo.getBar(); } ``` will be: ```java public t(){ String l = Nullability.castToNonnull(foo.getBar()); } ``` ### Instructions: Do not import any additional classes. Do not add Java comments, but also do not remove any preexisting comments. Only modify relevant lines as required. Do not address any other issues. Return only the modified method wrapped in the XML format shown below. Place the Java code inside the <code> tag using a CDATA block with ```java formatting. Just give me the fixed code snippet in XML format in <response> tag like the example below. I will apply the fix to my codebase. Do not import any additional libraries or classes or add java comments. If you can provide the fixed code snippet, please provide it in the following format and place the code snippet in the <code> tag within ```java block. <response> <success>true</success> <code><![CDATA[ ```java Your fixed code snippet here ``` ]]> </code> </response>