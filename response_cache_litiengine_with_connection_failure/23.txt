In the method below, the expression Game.loop() is a static accessor that simply returns the static field `gameLoop`: ```java public static IGameLoop loop() { return gameLoop; } ``` The value of `gameLoop` is controlled in `Game`: - During initialization (`Game.init(...)`): ```java gameLoop = new GameLoop("Main Update Loop", config().client().getMaxFps()); IGameLoop loop = loop(); if (loop != null) { loop.attach(physics()); loop.attach(world()); } ... initialized = true; ``` Here, `gameLoop` is explicitly set to a non-null instance before `initialized` becomes `true`. - During termination (`Game.terminate()`): ```java static synchronized void terminate() { if (!initialized) { return; } hasStarted = false; initialized = false; config().save(); gameLoop.terminate(); tweenEngine.terminate(); soundEngine.terminate(); world().clear(); ... gameLoop = null; screenManager = null; gameWindow = null; } ``` So `gameLoop` is set back to `null` when the game is terminated. Across the provided code, calls to `Game.loop()` are made in many places, often without null checks: - `GameWorld.loadEnvironment`: `Lock lock = Game.loop().getLock();` - `Emitter.activate/deactivate/update`: directly uses `Game.loop().attach/detach/getTickRate()` - `MovementController.handleMovement/handleForces`: `Game.loop().getDeltaTime()`, `Game.loop().getTimeScale()` - `GameTime` methods: `Game.loop().getTicks()`, `Game.loop().getTickRate()` - `GameMetrics.render`: `Game.loop().getUpdatableCount()` - And many other call sites that dereference `Game.loop()` without a null check. If `Game.loop()` could legally be `null` at those times, these call sites would throw `NullPointerException` immediately. The code base is written under the assumption that once the game is initialized and not yet terminated, `gameLoop` is non-null. The only time `gameLoop` is `null` (by construction) is: 1. Before `Game.init(...)` is called. 2. After `Game.terminate()` has run (where `gameLoop = null`). In both of those states, many of the shown methods (e.g., `GameWorld.loadEnvironment`, environment/entity update logic, UI, etc.) are part of the normal running game loop and assume an initialized game. Calling them before initialization or after termination is outside the intended lifecycle and would generally be erroneous usage. The library itself guards lifecycle transitions with `initialized` and `hasStarted` flags and uses `Game.init()` / `Game.start()` / `Game.terminate()` accordingly. Given this, the *logical contract* at these call sites is: - During the valid operational window (after successful `Game.init` and before `Game.terminate`), `gameLoop` is guaranteed to be non-null, so `Game.loop()` cannot return null. - A null `gameLoop` is only possible if: - The user calls into these API methods before initialization, or - The user calls into them after `terminate()` has run. Your question specifies *this specific call site* as `Game.loop()` in this codebase, which is being used throughout the engine’s runtime logic. Under the intended lifecycle usage of the engine, this invocation is not expected to see a null value and the implementation ensures `gameLoop` is non-null in that state. Therefore, at this call site, under the library's intended and guarded lifecycle (initialized and not terminated), `Game.loop()` does not return null. A null value would only arise in misuse scenarios (calling into runtime code before init or after terminate), which the surrounding engine design treats as invalid. is not nullable at the point it is used. ```java Game.loop() ``` Please rewrite the method so that at the line "@Override public void render(final Graphics2D g) { this.updateMetrics(); if (!Game.config().client().showGameMetrics()) { return; } this.currentOffsetY = 0; g.setColor(this.renderColor); // render client metrics this.drawTitle(g, "[client]"); this.drawMetric(g, "fps : " + this.getFramesPerSecond()); this.drawMetric(g, "max fps : " + this.maxFramesPerSecond); this.drawMetric(g, "updatables: " + Game.loop().getUpdatableCount()); // render jvm metrics if debug is enabled if (Game.config().debug().isDebugEnabled()) { this.drawTitle(g, "[jvm]"); this.drawMetric(g, "java : " + this.javaVersion); this.drawMetric(g, "memory : " + String.format("%-5.5s", this.usedMemory) + " MB"); this.drawMetric(g, "threads : " + Thread.activeCount()); } // render rendering metrics if (!this.renderMetrics.isEmpty()) { this.drawTitle(g, "[update]"); for (RenderMetrics metric : this.renderMetrics) { this.drawMetric(g, metric.toString()); } this.renderMetrics.clear(); } }", at the occurrence of: " this.drawMetric(g, "updatables: " + Game.loop().getUpdatableCount());" is replaced with `Nullability.castToNonnull(Game.loop())` See the example below, for expression `foo.getBar()`, wrap it with `Nullability.castToNonnull(foo.getBar())`. ```java public t(){ String l = foo.getBar(); } ``` will be: ```java public t(){ String l = Nullability.castToNonnull(foo.getBar()); } ``` ### Instructions: Do not import any additional classes. Do not add Java comments, but also do not remove any preexisting comments. Only modify relevant lines as required. Do not address any other issues. Return only the modified method wrapped in the XML format shown below. Place the Java code inside the <code> tag using a CDATA block with ```java formatting. Just give me the fixed code snippet in XML format in <response> tag like the example below. I will apply the fix to my codebase. Do not import any additional libraries or classes or add java comments. If you can provide the fixed code snippet, please provide it in the following format and place the code snippet in the <code> tag within ```java block. <response> <success>true</success> <code><![CDATA[ ```java Your fixed code snippet here ``` ]]> </code> </response>